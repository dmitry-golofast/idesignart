{
  "name": "idesignart",
  "version": "0.1.0",
  "private": true,
  "description": "idesignart — студия дизайна и 3D-визуализации (Nuxt + Payload + Tailwind v4 + TypeScript)",
  "scripts": {
    "dev": "pnpm kill-ports && pnpm --parallel --filter admin --filter web run dev",
    "dev:web": "pnpm --filter web dev",
    "dev:admin": "pnpm --filter admin dev",
    "build": "pnpm -r run build",
    "build:web": "pnpm --filter web build",
    "build:admin": "pnpm --filter admin build",
    "generate:types": "pnpm --filter payload generate:types",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "kill-ports": "powershell -Command \"Get-NetTCPConnection -LocalPort 3000,3001 -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }; Start-Sleep -Seconds 1; Write-Output 'Ports 3000,3001 cleared'\"",
    "clean": "pnpm -r exec rimraf .nuxt .output dist node_modules/.cache .next"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
